on:
  # The event respository_dispatch means on every API trigger,
  # this whole file gets executed
  repository_dispatch:
    types: [deploy-service]

jobs:
  update_file:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        env:
          PROPERTY_ID: ${{ github.context.payload.client_payload.propertyId }}
      - run: auto/bash $PROPERTY_ID

      - uses: actions/checkout@v3
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
      - run: |
          # propertyId=`echo $json | grep -o '"propertyId":"[^"]*' | grep -o '[^"]*$'`
          echo "$GITHUB_CONTEXT"
          # touch hello.md
